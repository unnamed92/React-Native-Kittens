# React-Native-Kittens

To start the project you need to:

- clone the repo

- run `npm install`

- run `cd ios`

- run `pod install`

- react-native run-ios - for ios emulator

- react-mative run-android - for android emulator
